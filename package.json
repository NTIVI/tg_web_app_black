{
  "name": "ntivi-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install prisma@5.22.0 --no-audit && npm install --prefix frontend --no-audit && npm run build --prefix frontend && npm install --prefix backend --no-audit && cd backend && npx prisma@5.22.0 generate",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}
