{
  "name": "tg-web-app-black-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "install-all": "npm install && npm --prefix frontend install && npm --prefix backend install"
  }
}
